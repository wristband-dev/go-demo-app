# React + TypeScript + Vite

This clientapp provides a minimal setup to get React working in Vite with HMR and some ESLint rules

This clientapp was built from:
```
npm create vite@latest clientapp -- --template react-ts
```
