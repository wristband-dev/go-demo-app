# React + TypeScript + Vite

This frontend provides a minimal setup to get React working in Vite with HMR and some ESLint rules

This frontend was built from:
```
npm create vite@latest frontend -- --template react-ts
```
